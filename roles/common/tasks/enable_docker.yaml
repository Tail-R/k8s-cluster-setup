- name: Enable and start Docker service
  systemd:
    name: docker
    enabled: yes
    state: started

- name: Add user to docker groups
  user:
    name: "{{ ansible_user }}"
    groups: docker
    append: yes
  when: ansible_user_id != 'root'